import {describe,expect,it} from "vitest";import {imageRequestSchema,sceneUpdateSchema,storyboardRequestSchema} from "./schemas";
const product={name:"x",description:"x",category:"x",audience:"x",sellingPoint:"x",offer:"",cta:"x",keyMessage:"x",concept:"x"};
describe("AI request validation",()=>{it("rejects unsupported duration",()=>expect(()=>storyboardRequestSchema.parse({product,creator:"n",duration:20})).toThrow());it("requires useful idempotency keys",()=>expect(()=>imageRequestSchema.parse({idempotencyKey:"short"})).toThrow());it("does not accept empty scene edits",()=>expect(()=>sceneUpdateSchema.parse({})).toThrow());it("accepts private reference paths",()=>expect(storyboardRequestSchema.safeParse({product,creator:"n",duration:12,referenceAssets:["ai/00000000-0000-4000-8000-000000000000/references/file.png"]}).success).toBe(true));it("rejects remote reference URLs",()=>expect(storyboardRequestSchema.safeParse({product,creator:"n",duration:12,referenceAssets:["https://storage.example/file.png"]}).success).toBe(false));it("rejects paths outside the reference namespace",()=>expect(storyboardRequestSchema.safeParse({product,creator:"n",duration:12,referenceAssets:["ai/other/path.png"]}).success).toBe(false))});
