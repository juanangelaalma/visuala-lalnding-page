import {describe,expect,it,vi} from "vitest";vi.mock("server-only",()=>({}));import {normalizeAtlasStatus} from "./providers";
describe("Atlas normalization",()=>{it("normalizes completion and output shapes",()=>expect(normalizeAtlasStatus({data:{status:"completed",outputs:["https://a.test/x",{video:"https://a.test/y"}],billed_cost_usd:.03}})).toMatchObject({status:"succeeded",outputs:["https://a.test/x","https://a.test/y"],actualCostUsd:.03}));it("normalizes active and terminal aliases",()=>{expect(normalizeAtlasStatus({data:{status:"starting"}}).status).toBe("processing");expect(normalizeAtlasStatus({data:{status:"timeout"}}).status).toBe("failed");expect(normalizeAtlasStatus({data:{status:"canceled"}}).status).toBe("cancelled")});it("rejects missing or unknown polling status",()=>{expect(()=>normalizeAtlasStatus({data:{}})).toThrow();expect(()=>normalizeAtlasStatus({data:{status:"mystery"}})).toThrow("Unsupported")});it("never succeeds without output",()=>expect(()=>normalizeAtlasStatus({data:{status:"succeeded"}})).toThrow("without output"));it("does not trust undocumented cost",()=>expect(normalizeAtlasStatus({data:{status:"queued",cost:9}}).actualCostUsd).toBeUndefined())});
