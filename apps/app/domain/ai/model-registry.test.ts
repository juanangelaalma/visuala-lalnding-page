import {describe,expect,it} from "vitest";import {atlasVideoRegistry,resolutionFor,selectImageModel,selectVideoModel} from "./model-registry";
describe("AI model routing",()=>{it("routes overlay scenes locally",()=>expect(selectVideoModel({sceneType:"cta",motionComplexity:"high"},"premium")).toBe("video_animated_image"));it("routes complex and talking scenes",()=>{expect(selectVideoModel({sceneType:"complex_hand_interaction",motionComplexity:"high"},"standard")).toBe("video_i2v_complex");expect(selectVideoModel({sceneType:"talking_to_camera",motionComplexity:"medium"},"standard")).toBe("video_talking_head")});it("uses references and quality",()=>{expect(selectImageModel(["https://x.test/a"])).toBe("image_reference_default");expect(resolutionFor("premium")).toBe("1080p")});it("rejects missing and incomplete registries",()=>{expect(()=>atlasVideoRegistry()).toThrow("ATLAS_VIDEO_MODELS_JSON");expect(()=>atlasVideoRegistry("{}")).toThrow("invalid")});it("uses configured field contracts",()=>{const item={modelId:"vendor/model",estimatedCostUsdPerSecond:0.05,fields:{prompt:"text",image:"start_image"},capabilities:{duration:false,resolution:false,aspectRatio:false,audio:false}};const registry=atlasVideoRegistry(JSON.stringify({video_i2v_economy:item,video_i2v_default:item,video_i2v_complex:item,video_talking_head:item,video_i2v_premium:item}));expect(registry.video_i2v_default.fields.image).toBe("start_image")})});
